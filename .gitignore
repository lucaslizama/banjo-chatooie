build/
*.d
*.o
*.elf
*.bin
*.map
*.nrm
*.so
*.dll
*.dylib
.vscode/settings.json

# Prebuilt binary, fetched from the N64Recomp releases page.
# See README.md -- build.sh prints the URL if it's missing.
tools/RecompModTool
tools/RecompModTool.exe

# Belt and braces: the helper stores its OAuth token in ~/.config, but never
# let one land in the repo even if someone points --config at the tree.
twitch_redemptions.json
*.token
